<!DOCTYPE html><html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Happy Birthday My Love 💖</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Poppins:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: 'Poppins', sans-serif;
      background: radial-gradient(circle at top, #ffd1dc, #ff9a9e);
      min-height: 100vh;
      overflow-x: hidden;
    }
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .card {
      background: rgba(255,255,255,0.95);
      max-width: 480px;
      width: 100%;
      border-radius: 24px;
      box-shadow: 0 30px 60px rgba(0,0,0,0.25);
      padding: 30px 26px 36px;
      text-align: center;
      animation: floatIn 1.2s ease;
      position: relative;
    }
    @keyframes floatIn {
      from { opacity: 0; transform: translateY(40px) scale(0.95); }
      to { opacity: 1; transform: translateY(0) scale(1); }
    }
    .badge {
      position: absolute;
      top: -14px;
      left: 50%;
      transform: translateX(-50%);
      background: linear-gradient(135deg, #ff4b6e, #ff758c);
      color: #fff;
      padding: 6px 16px;
      border-radius: 20px;
      font-size: 12px;
      letter-spacing: 1px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.2);
    }
    h1 {
      font-family: 'Playfair Display', serif;
      font-size: 34px;
      color: #ff4b6e;
      margin-top: 18px;
      margin-bottom: 6px;
    }
    .subtitle {
      font-size: 14px;
      color: #666;
      margin-bottom: 22px;
    }
    .photo {
      width: 180px;
      height: 180px;
      margin: 0 auto 18px;
      border-radius: 50%;
      background: url('your-photo.jpg') center/cover no-repeat;
      border: 6px solid #fff;
      box-shadow: 0 12px 30px rgba(255,75,110,0.45);
      position: relative;
    }
    .photo::after {
      content: '❤';
      position: absolute;
      bottom: -6px;
      right: -6px;
      background: #ff4b6e;
      color: #fff;
      width: 36px;
      height: 36px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 18px;
      box-shadow: 0 6px 16px rgba(0,0,0,0.25);
    }
    .message {
      font-size: 15px;
      line-height: 1.8;
      color: #444;
      margin-bottom: 22px;
    }
    .message strong { color: #ff4b6e; }
    .cta {
      background: linear-gradient(135deg, #ff4b6e, #ff758c);
      color: #fff;
      border: none;
      padding: 14px 26px;
      border-radius: 30px;
      font-size: 15px;
      cursor: pointer;
      box-shadow: 0 12px 26px rgba(255,75,110,0.45);
      transition: transform 0.25s, box-shadow 0.25s;
    }
    .cta:hover {
      transform: translateY(-2px);
      box-shadow: 0 18px 34px rgba(255,75,110,0.6);
    }
    .surprise {
      margin-top: 24px;
      display: none;
      animation: reveal 0.8s ease;
    }
    @keyframes reveal {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .letter {
      background: #fff5f7;
      border-radius: 16px;
      padding: 18px 16px;
      font-size: 14px;
      line-height: 1.8;
      color: #444;
      box-shadow: inset 0 0 0 1px rgba(255,75,110,0.15);
    }
    .signature {
      margin-top: 10px;
      font-family: 'Playfair Display', serif;
      font-size: 16px;
      color: #ff4b6e;
    }
    /* Floating hearts */
    .heart {
      position: fixed;
      bottom: -20px;
      font-size: 18px;
      animation: fly 6s linear infinite;
      opacity: 0.7;
    }
    @keyframes fly {
      0% { transform: translateY(0) scale(1); opacity: 0; }
      10% { opacity: 0.8; }
      100% { transform: translateY(-110vh) scale(1.6); opacity: 0; }
    }
  </style>
</head>
<body>
  <div class="hero">
    <div class="card">
      <div class="badge">FOR YOU ONLY</div>
      <h1>Happy Birthday</h1>
      <div class="subtitle">To the girl who makes my world beautiful</div><div class="photo" id="photo"></div>

  <div class="message">
    Aaj ka din mere liye bhi special hai,<br />
    kyunki <strong>tum ho</strong> 💫<br />
    Tumhari ek smile meri poori duniya roshan kar deti hai.
  </div>

  <button class="cta" onclick="openSurprise()">Open Your Surprise 🎁</button>

  <div id="surprise" class="surprise">
    <div class="letter">
      Happy Birthday meri jaan ❤️<br />
      Tumhari har khushi meri dua hai,<br />
      aur tumhari har muskaan meri jeet 🌸<br />
      Aaj nahi, har din tumhe special feel karana chahta hoon.
      <div class="signature">— Yours, Always</div>
    </div>
  </div>
</div>

  </div>  <script>
    const photos = [
      'baby-fountain.jpg',      // bachpan – fountain hairstyle
      'fairy-white.jpg',        // white pari dress
      'fairy-pink.jpg',         // pink pari + blush
      'selfie1.jpg',
      'selfie2.jpg',
      'portrait1.jpg',
      'portrait2.jpg'
    ];
    let index = 0;
    const photoDiv = document.getElementById('photo');
    photoDiv.style.backgroundImage = `url(${photos[0]})`;

    setInterval(() => {
      index = (index + 1) % photos.length;
      photoDiv.style.opacity = 0;
      setTimeout(() => {
        photoDiv.style.backgroundImage = `url(${photos[index]})`;
        photoDiv.style.opacity = 1;
      }, 500);
    }, 2500);

    function openSurprise() {
      document.getElementById('surprise').style.display = 'block';
      for (let i = 0; i < 30; i++) createHeart();
    }
    function createHeart() {
      const heart = document.createElement('div');
      heart.className = 'heart';
      heart.innerHTML = '❤';
      heart.style.left = Math.random() * 100 + 'vw';
      heart.style.animationDuration = (4 + Math.random() * 3) + 's';
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 7000);
    }
  </script></body>
</html>